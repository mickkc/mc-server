services:
  mc-server:
    build: .
    container_name: mc-server
    ports:
      - "${CONTAINER_PORT:-25565}:${SERVER_PORT:-25565}"
    volumes:
      - mc-data:/app/data
    env_file: .env
    restart: unless-stopped
    
volumes:
  mc-data: